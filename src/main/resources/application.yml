spring:
  # 环境配置
  profiles:
    active: dev-h2-plus
#    active: dev
  # 页面配置
  thymeleaf:
    mode: HTML5
    encoding: utf-8
    servlet:
      content-type: text/html
    cache: false

logging:
  config: classpath:logging-spring.xml
server:
  port: 9099
